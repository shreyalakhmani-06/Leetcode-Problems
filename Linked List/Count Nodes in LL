class Solution {
  public:
    int getCount(Node* head) {
        int cnt=0;
        Node*temp=head;
        while(temp!=NULL){
            cnt+=1;
            temp=temp->next;
        }
        return cnt;
    }
};
